## Upload
python3 -m pip install --upgrade build
python3 -m build
python3 -m pip install --upgrade twine
python3 -m twine upload --repository testpypi dist/*

## Install
python3 -m pip install -i https://test.pypi.org/simple/ CloudCrypt==0.0.1
